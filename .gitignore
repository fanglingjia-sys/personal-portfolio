# Generated site output — rebuild locally via 启动编辑.bat
_portfolio_site/

# Python cache
__pycache__/
*.pyc

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Temp
*.log
*.tmp

# Per-project backup of original (uncompressed) interaction docs; kept local
# so the user can restore if needed, but excluded from git to keep the
# repo small. To restore: copy .backup/交互文档.orig.jpg back over the
# project's 交互文档.jpg.
.backup/
**/.backup/
